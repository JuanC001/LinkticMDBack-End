import { request, response } from "express";

const userController = {}

userController.createReservation = (req = request, res = response) => { }

userController.updateReservation = (req = request, res = response) => { }

userController.deleteReservation = (req = request, res = response) => { }

userController.getReservations = (req = request, res = response) => { }

export default userController;